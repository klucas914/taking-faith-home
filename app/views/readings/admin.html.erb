<h1>Taking Faith Home</h1>
<h3>Share | Read | Talk | Pray | Bless</h3>
 

<table>
  <tr>
    <th>Week</th>
    <th>Day</th>
    <th>Title</th>
  </tr>
 
  <% @readings.each do |reading| %>
    <tr>
      <td><%= reading.week %></td>
      <td><%= reading.date %></td>
      <td><%= reading.title %></td>
      <td><%= link_to 'Begin Faith 5', share_reading_path(reading) %></td>
      <td><%= link_to 'Edit', edit_reading_path %></td>
      <td><%= ink_to 'Destroy', reading_path(reading),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "Creative Response"%> | | <%= link_to "Verse of the Week"%> | | <%= link_to "Mealtime Prayer" %> | | </p>
